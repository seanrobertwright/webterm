---
phase: 02-settings-ui-refinements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/settings/SettingsPanel.tsx
  - frontend/src/components/layout/Header.tsx
autonomous: true

must_haves:
  truths:
    - "Settings panel uses theme CSS variables instead of hardcoded gray/green colors"
    - "ThemeSelector dropdown appears inside the settings panel"
    - "ThemeSelector no longer appears in the header bar"
    - "Old Color Theme button list section is removed from settings"
    - "Font Size slider and Font Family dropdown still work correctly"
  artifacts:
    - path: "frontend/src/components/settings/SettingsPanel.tsx"
      provides: "Restyled settings panel with ThemeSelector"
      contains: "ThemeSelector"
    - path: "frontend/src/components/layout/Header.tsx"
      provides: "Header without ThemeSelector"
  key_links:
    - from: "frontend/src/components/settings/SettingsPanel.tsx"
      to: "frontend/src/components/layout/ThemeSelector.tsx"
      via: "import and render"
      pattern: "import.*ThemeSelector"
---

<objective>
Restyle the settings panel to use theme CSS variables and move the ThemeSelector from the header into the settings panel, removing the old Color Theme button list.

Purpose: Make settings panel visually consistent with the theme system and consolidate all theme/appearance settings in one place. The header becomes cleaner with ThemeSelector removed.
Output: Restyled SettingsPanel.tsx with ThemeSelector embedded, simplified Header.tsx without ThemeSelector.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-settings-ui-refinements/02-CONTEXT.md
@.planning/phases/02-settings-ui-refinements/02-RESEARCH.md
@frontend/src/components/settings/SettingsPanel.tsx
@frontend/src/components/layout/Header.tsx
@frontend/src/components/layout/ThemeSelector.tsx
@frontend/src/stores/settings-store.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle SettingsPanel with theme variables and embed ThemeSelector</name>
  <files>frontend/src/components/settings/SettingsPanel.tsx</files>
  <action>
Rewrite SettingsPanel.tsx with the following changes:

**1. Remove old Color Theme section entirely.**
Delete the entire "Theme" section (lines 98-119) that maps over `themeNames` with button list. Remove the `themeNames` import from `../../config/terminal-themes` (keep `fontFamilyOptions` import). Remove `themeName` and `setThemeName` from the useSettingsStore destructure — the `themeName` store field is still used by TerminalPane.tsx for xterm themes, but SettingsPanel no longer needs it.

**2. Add ThemeSelector import and render it.**
Add `import { ThemeSelector } from '../layout/ThemeSelector';` at the top. Render `<ThemeSelector />` as a new section in the settings content, labeled "UI Theme". Place it AFTER the font settings. The ThemeSelector currently renders as a relative-positioned dropdown — it should work inside the panel, but verify the dropdown positioning works within the scrollable settings area.

**3. Replace all hardcoded colors with theme CSS variables.**
Apply these replacements throughout the component:

| Old class | New class |
|-----------|-----------|
| `bg-gray-900` | `bg-card` |
| `border-gray-700` | `border-border` |
| `text-green-400` | `text-primary` |
| `text-gray-400` | `text-muted-foreground` |
| `text-gray-300` | `text-foreground` |
| `text-gray-500` | `text-muted-foreground` |
| `bg-gray-800` | `bg-secondary` |
| `border-gray-600` | `border-border` |
| `text-gray-200` | `text-foreground` |
| `focus:ring-green-500` | `focus:ring-ring` |
| `accent-green-500` | `accent-primary` |
| `hover:text-white` | `hover:text-foreground` |

Keep the panel structure (backdrop, slide-out, header, scrollable content) the same. Keep Font Size slider and Font Family dropdown as-is functionality-wise — just restyle their colors.

**Important:** The `themeNames` import is from `terminal-themes.ts` which provides xterm.js terminal color schemes — this is a DIFFERENT system from the UI themes in ThemeProvider/ThemeSelector. The old button list was for xterm themes. The ThemeSelector is for UI themes. They are separate concerns. Only remove the xterm theme UI from settings; do NOT remove the `themeName` field from the store.
  </action>
  <verify>
1. `npx tsc --noEmit -p frontend/tsconfig.json` — no type errors
2. Grep SettingsPanel.tsx for `gray-` — should return zero matches (all replaced with theme vars)
3. Grep SettingsPanel.tsx for `green-` — should return zero matches
4. Grep SettingsPanel.tsx for `ThemeSelector` — should find import and render
5. Grep SettingsPanel.tsx for `themeNames` — should return zero matches (removed)
  </verify>
  <done>SettingsPanel uses only theme CSS variables (no hardcoded gray/green), ThemeSelector rendered inside panel, old Color Theme button list removed, font settings unchanged functionally</done>
</task>

<task type="auto">
  <name>Task 2: Remove ThemeSelector from Header</name>
  <files>frontend/src/components/layout/Header.tsx</files>
  <action>
In Header.tsx:

1. Remove the `import { ThemeSelector } from './ThemeSelector';` line
2. Remove the `<ThemeSelector />` element from the right-side div (currently between ConnectionStatus and the settings gear button)
3. Keep everything else in Header.tsx unchanged — ConnectionStatus, session name editing, menu button, settings gear button all stay

The right-side div should now contain only ConnectionStatus and the settings gear button.

Note: Plan 02-01 (Wave 1, parallel) also modifies Header.tsx to replace the logo. Both plans touch Header.tsx, but in different sections (logo is in the left div, ThemeSelector is in the right div). If executing sequentially, the second plan should re-read the file. If there's a merge conflict, the changes are straightforward to combine.
  </action>
  <verify>
1. `npx tsc --noEmit -p frontend/tsconfig.json` — no type errors
2. Grep Header.tsx for `ThemeSelector` — should return zero matches
3. Header.tsx still contains ConnectionStatus and settings gear button
  </verify>
  <done>ThemeSelector completely removed from Header. Header shows: logo + menu (left), session name (center), connection status + settings gear (right).</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- No hardcoded gray-*/green-* colors in SettingsPanel.tsx
- ThemeSelector appears in SettingsPanel.tsx, not in Header.tsx
- Old themeNames button list gone from SettingsPanel
- Font settings still present and functional
</verification>

<success_criteria>
- Settings panel is fully themed with CSS variables
- ThemeSelector is the only way to change UI themes, and it lives in settings
- Header is cleaner without the theme dropdown
- No type errors across the project
</success_criteria>

<output>
After completion, create `.planning/phases/02-settings-ui-refinements/02-02-SUMMARY.md`
</output>
