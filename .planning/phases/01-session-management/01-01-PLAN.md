---
phase: 01-session-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/src/services/session-service.ts
  - backend/src/api/routes/sessions.ts
  - backend/src/api/rest-router.ts
  - frontend/src/services/session-api.ts
  - frontend/src/components/session/ClearAllDialog.tsx
  - frontend/src/components/session/SessionPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User can click Clear All Sessions in the session panel and see a confirmation dialog"
    - "Confirming the dialog deletes all sessions except the currently active one"
    - "Canceling the dialog does nothing"
    - "After clearing, the session list refreshes to show only the active session"
  artifacts:
    - path: "backend/src/services/session-service.ts"
      provides: "deleteAllSessions(excludeId) method"
      contains: "deleteAllSessions"
    - path: "backend/src/api/routes/sessions.ts"
      provides: "handleClearAllSessions route handler"
      contains: "handleClearAllSessions"
    - path: "frontend/src/components/session/ClearAllDialog.tsx"
      provides: "Confirmation dialog component"
      exports: ["ClearAllDialog"]
    - path: "frontend/src/services/session-api.ts"
      provides: "clearAllSessions API call"
      contains: "clearAllSessions"
  key_links:
    - from: "frontend/src/components/session/SessionPanel.tsx"
      to: "ClearAllDialog.tsx"
      via: "state toggle + button click"
      pattern: "ClearAllDialog"
    - from: "frontend/src/services/session-api.ts"
      to: "DELETE /api/v1/sessions"
      via: "fetch call"
      pattern: "fetch.*api/v1/sessions"
    - from: "backend/src/api/routes/sessions.ts"
      to: "session-service.ts"
      via: "deleteAllSessions call"
      pattern: "sessionService\\.deleteAllSessions"
---

<objective>
Add "Clear All Sessions" with confirmation dialog to the session panel.

Purpose: Let users quickly clean up accumulated sessions, keeping only their active session.
Output: Working bulk delete with backend endpoint, confirmation UI, and session list refresh.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-session-management/01-RESEARCH.md

@backend/src/services/session-service.ts
@backend/src/api/routes/sessions.ts
@backend/src/api/rest-router.ts
@frontend/src/services/session-api.ts
@frontend/src/components/session/SessionPanel.tsx
@frontend/src/components/session/SessionList.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend bulk delete endpoint</name>
  <files>
    backend/src/services/session-service.ts
    backend/src/api/routes/sessions.ts
    backend/src/api/rest-router.ts
  </files>
  <action>
1. In `session-service.ts`, add a `deleteAllSessions(excludeId: string): number` method:
   - Query all session IDs where `id != excludeId`
   - For each session, kill all PTYs using `ptyService.kill(paneId)` (follow the existing `deleteSession` pattern for PTY cleanup)
   - Execute `DELETE FROM sessions WHERE id != ?` (CASCADE handles windows/panes)
   - Return the count of deleted sessions

2. In `sessions.ts`, add a `handleClearAllSessions` handler:
   - Accepts body with `{ keepSessionId: string }`
   - Validates `keepSessionId` is present (throw `ValidationError` if missing)
   - Calls `sessionService.deleteAllSessions(keepSessionId)`
   - Returns `{ deleted: number }` with status 200
   - Follow the existing `handleDeleteSession` pattern for error handling

3. In `rest-router.ts`, register the route:
   - Add `registerRoute('DELETE', '/api/v1/sessions', handleClearAllSessions)`
   - Register this BEFORE the `/:id` route patterns to avoid `sessions` being matched as an ID param
  </action>
  <verify>
Run `npm run typecheck` to confirm no type errors. Manually verify the route registration order in rest-router.ts.
  </verify>
  <done>Backend accepts `DELETE /api/v1/sessions` with `keepSessionId` body, deletes all other sessions and their PTYs, returns deleted count.</done>
</task>

<task type="auto">
  <name>Task 2: Clear All confirmation dialog and session panel wiring</name>
  <files>
    frontend/src/components/session/ClearAllDialog.tsx
    frontend/src/components/session/SessionPanel.tsx
    frontend/src/services/session-api.ts
  </files>
  <action>
1. In `session-api.ts`, add a `clearAllSessions(keepSessionId: string): Promise<{ deleted: number }>` function:
   - `DELETE` request to `/api/v1/sessions` with JSON body `{ keepSessionId }`
   - Follow existing fetch patterns in the file

2. Create `ClearAllDialog.tsx`:
   - Props: `onConfirm: () => void`, `onCancel: () => void`, `sessionCount: number`
   - Fixed overlay with `z-60` (above SessionPanel's `z-50`) and `bg-black/70` backdrop
   - Dark themed card matching existing session panel styling (use theme-aware classes from existing components)
   - Display: "This will permanently delete {sessionCount} session(s). Your active session will be kept."
   - Two buttons: Cancel (text style) and Delete All (red bg, `bg-red-600 hover:bg-red-500`)
   - Use the styling patterns from the research (bg-gray-900, border-gray-700, rounded-lg, etc.) but check SessionPanel.tsx for theme-aware patterns and match those

3. In `SessionPanel.tsx`:
   - Add state: `showClearAll: boolean` (useState, default false)
   - Add a "Clear All" button in the session list header area (only show when there are 2+ sessions)
   - Wire the button to show ClearAllDialog
   - On confirm: call `clearAllSessions(activeSessionId)`, refresh the session list, close dialog
   - On cancel: close dialog
   - Pass `sessionCount` = total sessions minus 1 (excluding active)
  </action>
  <verify>
Run `npm run typecheck` and `npm run lint` to confirm no errors. Start dev servers with `npm run dev` and verify: open session panel, see Clear All button when multiple sessions exist, click it, see confirmation dialog, cancel closes it.
  </verify>
  <done>Clear All button appears in session panel when 2+ sessions exist. Clicking shows confirmation dialog. Confirming deletes all non-active sessions and refreshes the list. Canceling closes the dialog with no side effects.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- `npm run lint` passes
- Backend: `DELETE /api/v1/sessions` with valid body returns `{ deleted: N }`
- Frontend: Clear All button visible with multiple sessions, dialog opens/closes, bulk delete works
</verification>

<success_criteria>
User can open the session panel, click "Clear All Sessions", see a confirmation dialog showing how many sessions will be deleted, confirm to delete all non-active sessions, and see the list refresh with only the active session remaining.
</success_criteria>

<output>
After completion, create `.planning/phases/01-session-management/01-01-SUMMARY.md`
</output>
