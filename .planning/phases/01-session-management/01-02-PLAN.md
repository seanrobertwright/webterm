---
phase: 01-session-management
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/terminal/Terminal.tsx
  - frontend/src/services/export-service.ts
  - frontend/src/components/session/SessionList.tsx
  - shared/types/models.ts
autonomous: true

must_haves:
  truths:
    - "User can click Export on a session row to download a JSON file"
    - "Exported JSON contains session structure (name, windows, panes, layouts)"
    - "Exported JSON contains scrollback for currently active session's panes"
    - "Exporting a non-active session produces valid JSON with empty scrollback"
  artifacts:
    - path: "frontend/src/components/terminal/Terminal.tsx"
      provides: "serialize() method on TerminalHandle"
      contains: "serialize"
    - path: "frontend/src/services/export-service.ts"
      provides: "exportSession function and SessionExport type"
      exports: ["exportSession", "SessionExport"]
    - path: "shared/types/models.ts"
      provides: "SessionExport interface"
      contains: "SessionExport"
  key_links:
    - from: "frontend/src/components/session/SessionList.tsx"
      to: "export-service.ts"
      via: "Export button onClick"
      pattern: "exportSession"
    - from: "frontend/src/services/export-service.ts"
      to: "globalThis.terminalHandles"
      via: "serialize() calls to collect scrollback"
      pattern: "terminalHandles.*serialize"
---

<objective>
Add per-session JSON export with terminal scrollback capture.

Purpose: Let users save session structure and scrollback to a JSON file for archival or later import.
Output: Export button on session rows, client-side JSON download with scrollback.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-session-management/01-RESEARCH.md

@frontend/src/components/terminal/Terminal.tsx
@frontend/src/components/terminal/TerminalPane.tsx
@frontend/src/hooks/useTerminal.ts
@frontend/src/components/session/SessionList.tsx
@frontend/src/services/session-api.ts
@shared/types/models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose serialize() on TerminalHandle and define SessionExport type</name>
  <files>
    frontend/src/components/terminal/Terminal.tsx
    shared/types/models.ts
  </files>
  <action>
1. In `Terminal.tsx`, add `serialize` to the `TerminalHandle` interface:
   ```
   serialize: () => string | null;
   ```
   In the `useImperativeHandle` call, add:
   ```
   serialize: () => serializeTerminal() ?? null,
   ```
   where `serializeTerminal` comes from the `useTerminal` hook (check the hook's return value — it already has a serialize function from the SerializeAddon). If the hook returns it under a different name, use that name. The key is to forward the SerializeAddon's `serialize()` output through the TerminalHandle ref.

2. In `shared/types/models.ts`, add the `SessionExport` interface:
   ```typescript
   export interface SessionExportPane {
     id: string;          // original pane ID (used for scrollback lookup)
     shell: ShellType;
     cwd: string | null;
     cols: number;
     rows: number;
     title: string;
   }

   export interface SessionExportWindow {
     name: string;
     index: number;
     layout: Layout;
     panes: SessionExportPane[];
   }

   export interface SessionExport {
     version: 1;
     exportedAt: number;
     session: {
       name: string;
       windows: SessionExportWindow[];
     };
     scrollback: Record<string, string>;  // paneId -> serialized xterm string
   }
   ```
   Use existing `Layout` and `ShellType` types already defined in the file. Place these types near the other session-related types.
  </action>
  <verify>
Run `npm run build:shared` then `npm run typecheck` to confirm the new types compile and TerminalHandle update is valid.
  </verify>
  <done>TerminalHandle exposes `serialize()` returning the xterm serialized string or null. SessionExport type is defined in shared types.</done>
</task>

<task type="auto">
  <name>Task 2: Client-side export service and Export button in SessionList</name>
  <files>
    frontend/src/services/export-service.ts
    frontend/src/components/session/SessionList.tsx
  </files>
  <action>
1. Create `frontend/src/services/export-service.ts`:
   - Import `SessionExport` from shared types
   - Import `fetchSession` (or equivalent) from `session-api.ts` to get full session data
   - `collectScrollback(paneIds: string[]): Record<string, string>` function:
     - Iterate paneIds, call `globalThis.terminalHandles.get(paneId)?.serialize()` for each
     - Return map of paneId -> serialized string (skip nulls/undefined)
   - `exportSession(sessionId: string): Promise<void>` function:
     - Fetch full session data from API (GET /api/v1/sessions/:id)
     - Extract all pane IDs from all windows
     - Call `collectScrollback` with pane IDs
     - Build `SessionExport` object with version: 1, exportedAt: Date.now(), session structure, scrollback
     - Map the session API response to the SessionExport shape (name, windows with name/index/layout/panes)
     - For each pane include: id, shell, cwd, cols, rows, title
     - Download as JSON using Blob + URL.createObjectURL + anchor click + revokeObjectURL pattern
     - Filename: `{sessionName}-{timestamp}.json`
   - `downloadJson(data: unknown, filename: string): void` helper:
     - Create Blob with JSON.stringify(data, null, 2), type 'application/json'
     - Create anchor element, set href to object URL, set download attribute, click, cleanup

2. In `SessionList.tsx`:
   - Import `exportSession` from export-service
   - Add an Export button (download icon or text) to each session row
   - On click: call `exportSession(session.id)`
   - Add a brief loading/disabled state on the button during export (optional but nice)
   - Style consistently with existing action buttons in the session list rows
  </action>
  <verify>
Run `npm run typecheck` and `npm run lint`. Start dev servers, open session panel, verify Export button appears on each session row. Click Export on the active session — a JSON file should download. Inspect the JSON to verify it contains session structure and scrollback data.
  </verify>
  <done>Export button on each session row downloads a JSON file containing session name, window/pane structure, layout, and scrollback for currently mounted panes. Non-active sessions export with empty scrollback (valid JSON).</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- `npm run lint` passes
- Export button visible on each session row in the session panel
- Clicking Export on active session downloads JSON with structure + scrollback
- Clicking Export on non-active session downloads JSON with structure + empty scrollback
- Downloaded JSON has version: 1, valid session structure, scrollback record
</verification>

<success_criteria>
User can click Export on any session in the session panel. A JSON file downloads containing the session name, all windows with their layouts and pane configurations, and scrollback content for any currently rendered panes.
</success_criteria>

<output>
After completion, create `.planning/phases/01-session-management/01-02-SUMMARY.md`
</output>
