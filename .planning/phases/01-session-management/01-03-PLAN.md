---
phase: 01-session-management
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - backend/src/api/routes/sessions.ts
  - backend/src/api/rest-router.ts
  - backend/src/services/session-service.ts
  - frontend/src/services/import-service.ts
  - frontend/src/services/session-api.ts
  - frontend/src/components/session/SessionPanel.tsx
autonomous: true

must_haves:
  truths:
    - "User can click Import in the session panel to open a file picker"
    - "Selecting a valid exported JSON file creates a new session with the correct structure"
    - "Imported session has the correct number of windows, panes, and layout"
    - "Importing a session with a duplicate name auto-renames it"
    - "Scrollback from the export file is written into terminal panes after connection"
    - "Large export files (up to 10MB) import successfully"
  artifacts:
    - path: "backend/src/api/routes/sessions.ts"
      provides: "handleImportSession route handler"
      contains: "handleImportSession"
    - path: "frontend/src/services/import-service.ts"
      provides: "importSession function"
      exports: ["importSession"]
  key_links:
    - from: "frontend/src/components/session/SessionPanel.tsx"
      to: "import-service.ts"
      via: "Import button triggers file picker then importSession()"
      pattern: "importSession"
    - from: "frontend/src/services/import-service.ts"
      to: "POST /api/v1/sessions/import"
      via: "fetch call with parsed JSON"
      pattern: "fetch.*api/v1/sessions/import"
    - from: "backend/src/api/routes/sessions.ts"
      to: "session-service.ts"
      via: "createSession + window/pane manipulation"
      pattern: "sessionService\\.(createSession|createWindow)"
---

<objective>
Add session import from previously exported JSON files.

Purpose: Let users restore sessions from export files, recreating structure and replaying scrollback.
Output: Import button in session panel, backend import endpoint, session restoration with scrollback replay.
</objective>

<execution_context>
@C:/Users/seanr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/seanr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-session-management/01-RESEARCH.md
@.planning/phases/01-session-management/01-02-SUMMARY.md

@backend/src/services/session-service.ts
@backend/src/api/routes/sessions.ts
@backend/src/api/rest-router.ts
@frontend/src/services/session-api.ts
@frontend/src/services/export-service.ts
@frontend/src/components/session/SessionPanel.tsx
@frontend/src/components/session/SessionList.tsx
@shared/types/models.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend import endpoint</name>
  <files>
    backend/src/api/routes/sessions.ts
    backend/src/api/rest-router.ts
    backend/src/services/session-service.ts
  </files>
  <action>
1. In `rest-router.ts`, increase body size limit for the import route. The current limit is 1MB (`maxSize = 1024 * 1024` around line 117). Either:
   - Add a route-specific override mechanism: when the path matches `/api/v1/sessions/import`, use 10MB limit
   - Or add a `maxSize` parameter to `registerRoute` and pass 10 * 1024 * 1024 for the import route
   - The simplest approach: check if the `parseJsonBody` function accepts a size parameter; if so, pass a larger one for the import handler. If not, modify `parseJsonBody` to accept an optional `maxSize` parameter.

2. In `session-service.ts`, add an `importSession` method:
   ```
   importSession(data: SessionExport): SessionWithWindows
   ```
   - Create a new session using `createSession(data.session.name)` — this handles duplicate name auto-renaming
   - The auto-created first window needs to be handled (Pitfall 3 from research):
     - Get the auto-created session (it has 1 window with 1 pane)
     - For the FIRST imported window: reuse the auto-created window — update its name, layout, and configure panes
     - For additional imported windows: create new windows
   - For each imported window's panes beyond the first auto-created one: create additional panes using the existing split/create mechanisms
   - Each new pane should be configured with the imported shell type and working directory
   - Return the full created session with new IDs
   - Build and return a mapping of `oldPaneId -> newPaneId` so the frontend can replay scrollback to correct panes (include this in the response)

3. In `sessions.ts`, add `handleImportSession`:
   - Validate the body has `version: 1`, `session` object with `name` and `windows` array
   - Call `sessionService.importSession(data)`
   - Return the created session + pane ID mapping with status 201
   - Follow existing handler patterns for error handling

4. In `rest-router.ts`, register the route:
   - `registerRoute('POST', '/api/v1/sessions/import', handleImportSession)` with increased body limit
   - Register BEFORE the `/:id` routes to avoid pattern collision

NOTE: This is the most complex task. The key challenge is recreating the window/pane/layout structure from the export. Study the existing `createSession`, `createWindow`, and pane creation patterns carefully. The layout tree from the export should be applied directly since it uses the same `Layout` type. New pane IDs need to be substituted into the layout tree wherever old pDs appear.
  </action>
  <verify>
Run `npm run typecheck`. Test with curl: create a test JSON matching the SessionExport format and POST to `/api/v1/sessions/import`. Verify a new session is created with correct structure.
  </verify>
  <done>Backend accepts `POST /api/v1/sessions/import` with SessionExport JSON body (up to 10MB), creates a new session with correct window/pane/layout structure, returns created session + oldPaneId-to-newPaneId mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend import flow with file picker and scrollback replay</name>
  <files>
    frontend/src/services/import-service.ts
    frontend/src/services/session-api.ts
    frontend/src/components/session/SessionPanel.tsx
  </files>
  <action>
1. In `session-api.ts`, add `importSessionApi(data: SessionExport): Promise<{ session: SessionWithWindows; paneIdMap: Record<string, string> }>`:
   - POST to `/api/v1/sessions/import` with the parsed export data as JSON body
   - Return the created session and pane ID mapping

2. Create `frontend/src/services/import-service.ts`:
   - `openFilePicker(): Promise<File | null>` — create a hidden `<input type="file" accept=".json">`, trigger click, return selected file via Promise
   - `parseExportFile(file: File): Promise<SessionExport>` — read file text, JSON.parse, validate version === 1, validate session.name and session.windows exist, throw descriptive error on invalid format
   - `importSession(): Promise<void>` — orchestrates the full flow:
     a. Open file picker
     b. If no file selected, return
     c. Parse the file
     d. Call importSessionApi with parsed data
     e. Get back created session + paneIdMap
     f. Switch to the newly imported session (use existing session store's setActiveSession or equivalent)
     g. After the session loads and terminals mount, replay scrollback:
        - For each entry in the export's `scrollback` record, look up the new pane ID via paneIdMap
        - Get the terminal handle via `globalThis.terminalHandles.get(newPaneId)`
        - If handle exists, call `handle.write(scrollbackString)` to replay the scrollback
        - NOTE: terminals must be mounted first — may need a small delay or callback mechanism
     h. Handle errors with user-visible feedback (console.error at minimum, or a toast if the project has one)

3. In `SessionPanel.tsx`:
   - Add an "Import Session" button near the Clear All button / session list header area
   - On click: call `importSession()` from import-service
   - Show loading state during import
   - After successful import, refresh the session list
   - The button should always be visible (unlike Clear All which needs 2+ sessions)
  </action>
  <verify>
Run `npm run typecheck` and `npm run lint`. Start dev servers, export a session first (using Plan 02's export feature), then click Import, select the exported JSON file, verify a new session is created and appears in the session list with correct structure.
  </verify>
  <done>Import button in session panel opens file picker. Selecting a valid export JSON creates a new session with correct structure, switches to it, and replays scrollback into terminal panes. Invalid files show an error. Duplicate names are auto-renamed.</done>
</task>

</tasks>

<verification>
- `npm run typecheck` passes
- `npm run lint` passes
- Full round-trip: Export session -> Import the exported file -> new session created with same structure
- Imported session has correct number of windows and panes
- Layout matches the original session
- Scrollback is replayed into terminal panes after connection
- Large exports (many panes with scrollback) import without body size errors
- Duplicate session names are handled gracefully (auto-renamed)
</verification>

<success_criteria>
User can click Import in the session panel, select a previously exported JSON file, and have a new session created that mirrors the original session's structure (windows, panes, layout). Scrollback from the export is replayed into the new terminal panes. The imported session appears in the session list and can be switched to immediately.
</success_criteria>

<output>
After completion, create `.planning/phases/01-session-management/01-03-SUMMARY.md`
</output>
